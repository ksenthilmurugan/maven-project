pipeline {

  agent any
  
  stages { 
    stage("build") { 
      steps {
        echo "In Build section"
      }
    }
    
    
    stage("test") { 
      steps {
        echo "In test section"
      }
    }
    
    
    stage("deploy") { 
      steps {
        echo "In deploy section"
      }
    }
  }
}
